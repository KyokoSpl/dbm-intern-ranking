[package]
name = "actix-api"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4"
dotenv = "0.15.0"
serde = {version = "1.0.200", features = ["derive"]}
sqlx = { version = "0.7.4", features = [ "runtime-tokio", "mysql" ] }
# mysql = "25.0.0"
